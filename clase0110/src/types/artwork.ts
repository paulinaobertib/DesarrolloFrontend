export type ArtworkApiResponse = {
  albumId: number;
  id: number;
  title: string;
  url: string;
  thumbnailUrl: string;
};

export type Artwork = {
  id: number;
  title: string;
  imageUrl: string;
  thumbnailUrl: string;
};
